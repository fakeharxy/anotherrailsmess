<h1> <%= @past_page.title || "Untitled" %></h1>

<%= render @paragraphs %>

<script>
$(document).off('click', '.todo').on('click', '.todo', function () {
  var paraId = $(this).index('.todo') + 1;
  $.post('/pages/paragraph/todo/' + paraId, { page: <%= @past_page.id %> } );
});

$(document).off('click', '.important').on('click', '.important', function () {
  var paraId = $(this).index('.important') + 1;
  $.post('/pages/paragraph/important/' + paraId, { page: <%= @past_page.id %> } );
});
</script>
